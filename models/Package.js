// Class: Package
// Properties: tracking_id, size, destination
class Package {
  constructor(trackingId, size, destination) {
    this.trackingId = trackingId;
    this.size = size;
    this.destination = destination;
  }
}

module.exports = Package;
